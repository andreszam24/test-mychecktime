import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_movil_ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
